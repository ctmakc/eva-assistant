# EVA Personal Assistant - Environment Configuration
# Copy this to .env and fill in your values

# ===========================================
# REQUIRED: Security (generate random strings)
# ===========================================
API_SECRET_KEY=change-me-to-random-string-min-32-chars
VAULT_MASTER_KEY=change-me-to-another-random-string

# Admin password for initial setup (change after first login!)
ADMIN_PASSWORD=change-me-on-first-setup

# ===========================================
# OPTIONAL: API Keys (can be set via Admin API instead)
# ===========================================
# LLM Provider: "gemini" or "anthropic"
LLM_PROVIDER=gemini

# These can be left empty and configured via /api/v1/admin/settings
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=

# ===========================================
# NOTE: All API keys can be configured securely
# through the Admin API after deployment:
# POST /api/v1/admin/settings
# ===========================================
